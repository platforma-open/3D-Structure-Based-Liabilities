"""Shared pytest fixtures for the Structure-Based Liabilities test suite.

Module discovery (script source + `tests/` helpers) is wired via
`pyproject.toml` `[tool.pytest.ini_options].pythonpath`, so no manual
`sys.path` mutation is needed here. Per-test fixtures live alongside the
tests that need them; this file stays a placeholder for the future."""
